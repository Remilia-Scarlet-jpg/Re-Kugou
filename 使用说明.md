# RE:KG — 可视化音乐播放器(使用说明)

基于本地酷狗 API(源码版 KuGouMusicApi,概念版模式)+ 3D 音乐壁纸可视化的网页版音乐播放器。双击即用。

## 启动

1. 双击 **start.bat**
   - 自动启动 `api\app.js`(酷狗 API 源码服务,**--platform=lite 概念版模式**,端口 3000,已在运行则跳过)
   - 启动本地服务 `server.js`(静态页面 + 播放地址解析,端口 3001)
   - 打开浏览器访问 `http://localhost:3001`
2. 更新数据源:进入 `api\` 目录执行 `git pull && npm install`
3. 关闭:直接关掉两个窗口即可(播放队列已保存在浏览器本地)

> start.bat 必须保持 **纯 ASCII 编码**(cmd 在中文系统用 GBK 解析,UTF-8 中文注释会导致乱码执行失败)。
> 数据源必须用概念版模式:标准版模式下专辑/歌单搜索接口返回空列表(实测)。

## 安装版(Electron 安装包)

1. 运行 **REKG Setup x.y.z.exe**(NSIS 安装包)→ 可选安装目录(默认 `%LOCALAPPDATA%\Programs\REKG`;开始菜单 + 桌面快捷方式;主程序名为「REKG.exe」)
2. 双击「REKG」快捷方式启动;内嵌音乐服务自动拉起,**无需 Node/start.bat/浏览器**
3. 窗口为无边框:顶部透明条**拖动**、**双击最大化/还原**、右上角悬浮「─ ✕」胶囊(悬停显现)最小化/关闭
4. **桌面歌词小窗是系统级置顶无边框浮窗**(浏览器版没有的置顶能力),拖动/字号/透明度/位置记忆与网页版一致
5. 数据(播放队列/登录与 VIP/壁纸/视觉参数/小窗位置)在 `%APPDATA%\REKG`,**卸载不删除**,重装即恢复;旧版本(星际音乐)的数据目录会在首次启动时自动改名迁移,不丢失
6. 首次运行若弹出 Windows SmartScreen(应用未签名):更多信息 → 仍要运行
7. 退出安装版后本地 3000/3001 端口自动释放;若先开了浏览器版(start.bat)再启动安装版,会提示端口占用并退出——关掉浏览器版即可
8. 卸载:Windows 设置 → 应用 →「REKG 1.0.0」→ 卸载,或运行安装目录里的「Uninstall REKG.exe」;用户数据保留(见第 5 条)

> 仅保留一份服务:浏览器版与安装版共用 3000/3001 端口,请勿同时运行。

## 玩法

音乐壁纸布局:中间是随音乐实时变化的 3D 立体视觉,界面元素全部悬浮其上——左上角 logo 与右侧边缘「⌕」按钮都可开关音乐面板,面板内分「浏览 / 队列 / 视觉」三个标签页。

- **✨ 推荐**:每日推荐 30 首,点卡片即播
- **🏆 榜单**:55 个官方榜单 → 歌曲列表,整行点击播放
- **🎵 歌曲**:搜索歌名/歌手 → 歌曲卡片(歌手 · 时长),点卡片即播(词曲检索通道)
- **💿 专辑**:搜索歌手/专辑名 → 专辑 → 歌曲(带时长/封面/歌词)
- **🎶 歌单**:搜索主题歌单 → 点卡片即播(经 m.kugou 老接口,仅前 10 首)
- **📁 我的歌单**:粘贴歌单分享链接/歌单号添加,也可以直接输入歌单名搜索添加(见下方「我的歌单怎么用」;分享链接可读完整列表)
- **🗂 队列**:面板内「队列」标签页,当前播放高亮,点击任意行切歌;**点击播放条上的歌曲名**可直接打开面板并切到队列(歌单界面)
- **🔑 登录**:抽屉顶部用户大标题「未登录」→ 酷狗 App 扫码 → 显示 VIP 徽章与到期时间(见「登录与 VIP」)
- **歌词抽屉**:播放条「词」按钮,独立抽屉逐行高亮跟随、点击任意行跳转;抽屉头部显示当前歌曲封面(分享歌单等无封面歌曲会在播放时自动补全)
- **沉浸模式**:点进歌单/专辑/榜单的歌曲列表页后,面板隐藏搜索框与导航,整块右侧区域进入歌曲页面;点「‹ 返回」恢复
- **🎛 视觉 DIY 控制台**:面板「视觉」标签页,场景/运镜/壁纸/桌词共 19 项参数即时生效(含「粒子效果」总开关——可完全关掉背景星空粒子,关掉后画布隐藏且渲染循环挂起,省电),4 个命名预设槽一键保存/应用/清空,全部参数自动持久化(刷新不丢)
- **🎥 电影运镜**:节拍 kick + 闲时漂移 + FOV 推镜(快攻慢放、方向随机),幅度与开关可调;低性能时冲击自动降载
- **🖼 壁纸模式**:本地图片(自动压缩 WebP)或视频(≤1000MB)设为音乐壁纸,垫在 3D 星体视觉之下;透明度/缩放/模糊可调,刷新自动恢复
- **📝 桌面歌词**:播放条「桌词」按钮打开独立歌词小窗(**无背景**——只留文字浮在桌面上,同酷狗桌面歌词,文字带深色描边因此在任何壁纸上都清晰;可拖动、可缩放、字号/透明度可调,窗口最大不超过屏幕 80%),逐行高亮跟随播放(播放中途打开直接定位当前句),与抽屉歌词完全解耦
- **🔊 音量条数字**:播放条音量滑杆旁实时显示百分比数字,滑杆 / 静音钮 / 键盘 ↑↓ 三路全联动
- **📂 本地音频**:「📁 我的歌单」页顶部「▶ 播放本地文件」按钮 → 多选本地音频(mp3/flac/wav/m4a/aac/ogg/ape)→ 解析内嵌 ID3 标签(歌名/歌手/封面,失败回退文件名)并立即播放;文件存入浏览器 IndexedDB,刷新后队列自动恢复(播放时按需读取,单文件上限 100MB)
- **➕ 新建本地歌单**:「📁 我的歌单」页「＋ 新建本地歌单」按钮 → 多选文件 → 生成「xxx 等N首」本地歌单卡(纯本地,零网络请求);点卡片进入曲目页点播;移除歌单卡时自动清理未被其他歌单/队列引用的本地文件
- 全部界面元素为 **Mac Dock 风格无边框高透光玻璃**(透明度 16%-34%):播放条居中悬浮胶囊、歌词抽屉与浏览面板为悬浮圆角玻璃卡片,按钮/卡片/输入框均无边框线(输入聚焦为光晕提示),粒子效果可透过界面玻璃清晰可见;**右侧浏览抽屉上下留白收高,底边停在播放条上方**;面板可用 **Esc** 关闭,歌词抽屉与浏览面板独立开关
- 配色与玻璃质感参考 **Mineradio 2.1.0**:近黑中性底(#08090b)、薄荷青主色 #00f5d4 + 蓝 #2442ff、香槟金 #f4d28a、左上进光的对角渐变玻璃 + 顶部内侧高光 + 薄荷青辉光

## 快捷键

| 按键 | 功能 |
|---|---|
| 空格 | 播放 / 暂停 |
| ← / → | 上一首 / 下一首 |
| Ctrl + ← / → | 后退 / 前进 5 秒 |
| ↑ / ↓ | 音量 ±5% |

## 可视化

- **3D 场景**(WebGL,本地 three.js r158):中央音乐球体 + 两个倾斜光环 + 远星场 + 相机环绕视差
  - 低频 → 球体膨胀、两极拉伸;高频 → 赤道波纹与光环散大;节拍 → 冲击波环扩散 + 镜头脉冲
  - 配色参考 Mineradio Emily 预设:待机为薄荷青/青蓝(星场 #9db8cf),能量升高过渡到香槟金暖色;静音 3 秒自动回待机(呼吸律动)
- **电影运镜**:轨道 = 用户基准环绕 + 电影偏移(节拍 kick / 闲时漂移 / FOV 推镜),可在视觉面板关闭
- **壁纸模式**:本地图片/视频作为 DOM 媒体层垫在透明 WebGL 画布之下(2D 回退模式下被不透明星野遮挡,属设计行为)
- **回退**:WebGL 不可用时自动回退 2D 粒子星野(蓝紫星系盘 + 星屑迸发)
- 性能自适应:总粒子约 4900、帧率低于 45 时自动降像素比与远星数(只降不升),运镜冲击同步降载

## 架构

```
浏览器 :3001 ──静态页面──> server.js ──/api/url 代理──> trackercdn.kugou.com(解析 mp3 地址,128k)
    │                     (md5(hash+"kgcloudv2"), 加 CORS 头)
    │                     └──/api/playlist 代理──> m.kugou.com/plist/list/{id}(歌单前 10 首,手机 UA)
    │                          │                └─ wwwapi.kugou.com/share 分享页(完整曲目列表,App 短链落点)
    │                          │                └─ pubsongscdn.kugou.com H5 签名接口(新式概念版短链,全量曲目)
    ├──元数据/歌词/登录/VIP ──> api\app.js :3000(KuGouMusicApi 源码版,--platform=lite;自带 CORS+凭证)
    └──播放 mp3 CDN 地址(crossorigin="anonymous",Web Audio 频谱分析)
```

- `server.js`:零依赖静态服务 + `/api/url?hash=` 与 `/api/playlist?id=|url=` 两个代理(酷狗旧接口无 CORS,浏览器不能直连);分享链接三通道:老分享页内嵌 JSON(≤100 首)、m.kugou 歌单号(前 10 首)、新式概念版 H5 签名接口(全量)
- `api\`:KuGouMusicApi 源码服务(概念版模式,--platform=lite),提供推荐/榜单/搜索/歌词/登录/VIP 接口,自带 CORS+凭证
- `web/js/`:原生 ES 模块,无构建;`visualizer3d.js` 为 3D 可视化引擎(全局 THREE),`visualizer.js` 为 2D 回退引擎;`fx.js` 视觉参数中枢(订阅 + 防抖持久化)、`cameraman.js` 电影运镜纯数学、`fx-panel.js` DIY 控制台、`wallpaper.js` 壁纸(IndexedDB)、`local-music.js` 本地音乐库(IndexedDB `vmp-localmusic-v1` + 元数据 `vmp.localmusic.v1`)、`id3.js` ID3 标签解析(纯函数)、`desktop-lyrics.js`/`desktop-lyrics-view.js` 桌面歌词小窗(BroadcastChannel,墙钟时间戳同步);`config.js` 支持 `window.__APP_CONFIG__` 注入(Electron 壳预留)
- `web/js/vendor/three.min.js`:本地 vendored 的 three.js **r158 UMD 构建**(最后带 UMD 的版本,勿升级 r160+),运行时无 CDN 依赖

## 安全加固(2026-08)

- **仅监听本机**:两个服务(3000/3001)默认只绑定 `127.0.0.1`,局域网内其他设备无法访问
- **CORS 白名单**:API 服务只对 `localhost:3001` / `127.0.0.1:3001` 回跨域头(可用环境变量 `CORS_ALLOW_ORIGIN` 覆盖),恶意站点无法带凭证调用本地 API
- **模块白名单**:酷狗 API 源码服务只注册前端实际使用的 12 条路由(推荐/榜单/搜索/歌词/登录二维码/VIP 音源等),其余约 157 条(热搜、短信验证码、手机号登录、注册等)一律 404
- **SSRF 防护**:歌单链接代理只允许跳转 `kugou.com` 及其子域,伪装域名、伪子域、内网地址、file: 等一律拒绝
- **CSP + 安全头**:页面带内容安全策略(禁内联脚本/eval、白名单连接源),静态服务回 `X-Content-Type-Options` / `X-Frame-Options` / `Referrer-Policy`
- **登录 cookie 加固**:登录凭证由服务端写入 HttpOnly + SameSite=Lax + 一年有效期 cookie(前端脚本无法读取);退出登录由服务端统一过期 9 项 cookie
- **错误脱敏**:上游报错只返回简短提示,不含堆栈/凭证/内部路径;服务端日志只记路径与状态码
- 上游明文接口仅剩 trackercdn 一处,已切换 https;本地 API 依赖已裁剪(npm audit 0 漏洞)

## 登录与 VIP

1. 右上角点「**登录**」→ 弹出二维码 → 用**酷狗音乐 App** 扫码(我的 → 右上角扫一扫)→ 在手机上确认
2. 成功后按钮变为金色徽章:VIP 显示「👑 VIP · 到期时间」,非 VIP 显示「已登录」;**登录状态会持久保存,关闭重开应用无需重新登录**
3. 退出:点徽章一下变红(3 秒内再点一下确认),仅清除本机浏览器里的酷狗登录状态(服务端过期 cookie + 本地登录标记),不影响 App

> **VIP 歌曲**:登录后播放自动走登录音源接口(/song/url,v5),VIP 锁定曲也能拿到 **320k 明文 mp3** 地址(实测:未登录时该接口被风控拦截 20028,登录后对 VIP 曲目正常返回,CDN 带 CORS 头可直接接入频谱可视化)。注意 /song/url/new(v6)虽返回 320k 地址,但是 **.mgg 加密格式**,浏览器无法解码,已弃用。未登录时回退 128k 公开音源,VIP 锁定曲不可播。

## 我的歌单怎么用

1. 在**酷狗音乐 App** 里打开你自己的歌单 → 右上角「分享」→ 复制链接
2. 回到本应用「📁 我的歌单」→ 粘贴链接(或直接填歌单号)→ 点「添加」
3. **也可以直接输入歌单名**点「添加」→ 按名称搜索出结果,点卡片加入我的歌单
4. 添加的歌单保存在浏览器本地,点卡片即播;悬停卡片右上角 **✏️ 重命名**、**✕ 移除**

> 为什么登录了还要粘贴链接:酷狗歌单列表接口(cloudlist 服务)签名已失效,即使扫码登录也无法自动拉取自己的歌单,故「我的歌单」仍需粘贴分享链接添加。
>
> - **App 分享短链(t1.kugou.com 等)**:可读**完整曲目列表**;老式分享页链接最高 100 首(超出部分酷狗页面本身不带),新式概念版分享链接(页面跳转到 activity.kugou.com)走 H5 签名接口**全量读取(实测 145 首)**且每首自带封面;分享页没有歌单名,默认显示「分享歌单(n首)」,可用 ✏️ 重命名
> - **歌单号 / m.kugou 链接**:走 m.kugou 老接口,**只返回前 10 首**
> - **私有歌单均无法读取**,请先将歌单设为公开

## 测试

```bash
node scripts/cdp_test.js              # 全流程自动化测试(212 项,含抽屉/队列/沉浸/3D 渲染 S11 + 视觉面板 S12a/b + 运镜 S12d + 壁纸 S12e + 桌词小窗 S12c/f + 视频壁纸大文件 S14 + 登录态持久化 S15 + 歌单输入兜底 S16 + 歌曲搜索 S17 + 封面补全 S18 + 新式分享链接 S19 + 音量数字 S20 + 本地音频 S21 + ID3 解析 S21b + 本地歌单 S22 + 桌词三修复 S23 + 安全断言 S24(CORS 白名单/模块白名单/SSRF 拦截/安全头/Cookie 属性/CSP),需两服务运行、装有 Chrome)
node scripts/fps_check.js             # 帧率抽查
```

## 已知限制

- 部分歌曲为 VIP/版权锁定,播放失败会 toast 提示并自动切下一首(连续 3 首失败停止)
- 音质:未登录统一 128k 公开音源,VIP 锁定曲不可播;登录后走登录音源接口,VIP 曲目可播且最高 320k
- 歌单曲目:歌单号/普通链接仅前 10 首;App 分享短链可读完整列表(新式概念版链接全量、老式分享页最高 100 首);仅公开歌单;分享歌单的封面在播放时自动补全(老式分享页数据无封面,凭专辑号经 /images 接口补;新式链接自带封面)
- 登录仅用于展示 VIP 状态:歌单列表接口(cloudlist 服务)签名已失效,「我的歌单」无法自动同步,需粘贴分享链接
- 桌面歌词小窗:浏览器版是普通窗口、无系统级「置顶」能力(浏览器限制);安装版为无边框置顶浮窗(见「安装版」);拖动后的位置都会记住;窗口尺寸被钳制在屏幕 80% 内(防最大化后重开全屏)
- 本地音乐:元数据解析仅支持 mp3 内嵌 ID3 标签(v2.3/v2.4/v1),其余格式按文件名识别;本地歌无歌词与封面自动补全;单文件上限 100MB;移除歌单卡时会清理未被引用的本地文件(队列中/正在播放的保留)
- 壁纸模式在 2D 回退模式下不可见(2D 引擎底色不透明);视频壁纸上限 1000MB(IndexedDB 存储,浏览器模式受磁盘配额限制;大视频保存可能需要较长时间,请耐心等待提示)

## 版权声明

数据与音频均来自酷狗音乐,仅供学习交流,请尊重版权、勿用于商业用途。
